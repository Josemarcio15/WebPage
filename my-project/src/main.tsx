import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    {/* Pode criar seu componente aqui ou importar o que quiser */}
    <div>Hello world!</div>
  </React.StrictMode>
);
